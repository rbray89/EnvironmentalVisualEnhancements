To generate the asset bundles:

1. Open ../Shaders in Unity (use correct version for KSP)
2. Select "Assets/Build Asset Bundles" menu option.

Note that the the XML/eveshaders_bundle.xml file there can be generated using PartTools, or just edited manually, if new shaders are added to the bundle.